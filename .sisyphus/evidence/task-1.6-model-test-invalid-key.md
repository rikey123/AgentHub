# Task 1.6 model test invalid key

- Scenario: model-config test with invalid API key
- Result: failed as `invalid_api_key`
- Notes: response redacts provider secret material; polling job returns terminal failed state.
